-- SILLET CLIENT - VERSION COMPLETA
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 250, 0, 300)
MainFrame.Position = UDim2.new(0.5, -125, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)

local Title = Instance.new("TextLabel", MainFrame)
Title.Text = "SILLET CLIENT v1.0"
Title.Size = UDim2.new(0, 250, 0, 30)

local function addButton(text, callback)
    local btn = Instance.new("TextButton", MainFrame)
    btn.Size = UDim2.new(0, 230, 0, 40)
    btn.Text = text
    btn.Position = UDim2.new(0, 10, 0, #MainFrame:GetChildren() * 45)
    btn.MouseButton1Click:Connect(callback)
end

-- TUS FUNCIONES AQUÍ
addButton("Fly (Vuelo)", function() 
    -- Tu lógica de vuelo
end)

addButton("Speed Boost", function() 
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 50 
end)

addButton("Teleport al Aro", function()
    local hoop = workspace:FindFirstChild("Hoop")
    if hoop then game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = hoop.CFrame end
end)
